/**
 * Re-export from the canonical server client module.
 * All imports of "@/utils/supabase-server" continue to work without changes.
 */
export { createServerClient } from "@/lib/supabase/server";
